# Based on clangd-14

CompileFlags:
  Add:
    - -Wall
    - -Wextra
    - -march=native
  Remove:
    - -forward-unknown-to-host-compiler
    - --generate-code*

Diagnostics:
  ClangTidy:
    Add:
      # TODO: add more
      - bugprone-*
      - performance-*

Hover:
  ShowAKA: true
