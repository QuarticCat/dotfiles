# Based on clangd 14

CompileFlags:
  Add:
    - -Wall
    - -Wextra
    - -march=native
  Remove:
    # Unrecognized nvcc flags
    - -forward-unknown-to-host-compiler
    - --generate-code*

Hover:
  ShowAKA: true
